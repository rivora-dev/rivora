/target
**/*.rs.bk
.DS_Store
.rivora/data/
*.swp
*.swo
.idea/
.vscode/

# Accidental / non-Rust dependency trees (not part of Rivora)
node_modules/
# Local JS tooling experiments (e.g. wrangler) — not Rivora release artifacts
/package.json
/bun.lock
/package-lock.json
/yarn.lock

# Install-worker generated embed (regenerated by distribution/install-worker/build.mjs)
distribution/install-worker/src/script.generated.js
distribution/install-worker/.wrangler/

# Local packaging / release scratch
/dist/
*.tar.gz
SHA256SUMS

# Internal working docs (local only; not tracked).
# Authoritative architectural invariants for contributors live at:
#   docs/ARCHITECTURAL_INVARIANTS.md
docs/internal/
